/// Defines the scroll behavior for CustomInteractiveViewer.
enum ScrollMode {
  /// Allow scrolling in both horizontal and vertical directions.
  both,
  
  /// Allow scrolling only in the horizontal direction.
  horizontal,
  
  /// Allow scrolling only in the vertical direction.
  vertical,
  
  /// Disable scrolling in both directions.
  none,
}